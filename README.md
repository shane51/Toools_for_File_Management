Toools_for_File_Management
==========================

Collection of useful file management related tools

1. ungzip.rb # used for extract *.tar.gz files to disk(can management file size bigger than 1 GB).
